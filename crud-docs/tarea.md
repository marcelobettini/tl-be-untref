## Documentación a implementar en el archivo README.md
- debes detallar al inicio de la documentación la URL base. Ejemplo: http://localhost:3008/api/v1/
- deberás crear un ejemplo de uso de cada uno de los métodos 
  - GET
  - POST
  - PATCH
  - DELETE
- incluye un ejemplo de código del cuerpo del mensaje para los métodos POST - PATCH
- incluye un ejemplo del archivo .env para explicar cómo definir la conexión a MongoDB

### Debes integrar en la creación de esta documentación:
1. títulos y subtítulos, más formateo estándar de párrafos con Markdown
2. integrar un sumario en el encabezado del documento Markdown
3. una tabla en formato Markdown que represente los endpoint a y la ruta base de cada uno
4. agrega código de ejemplo de lo que hay que enviar en el cuerpo al usar los métodos POST y PATCH
5. utilizar el formato código, para representar los bloques de código de ejemplo
