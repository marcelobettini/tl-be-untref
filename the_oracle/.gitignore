# THE ORACLE — local ignores (the root .gitignore already covers node_modules and .env)
# Coverage / test artifacts
coverage/
*.lcov

# Hermes meta-trabajo (PLAN, .hermes/state, etc.) — no es código del feature
.hermes/
